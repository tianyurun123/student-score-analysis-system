# 学生成绩管理与分析系统 - 功能说明

## 系统概述

本系统是一个基于Django + Vue的学生成绩管理与分析系统，支持大学生课程成绩的全面管理，包括成绩录入、计算、分析和打印等功能。

## 核心功能

### 1. Excel成绩导入功能 ✅

**功能描述：**
- 支持上传Excel文件（.xlsx, .xls格式）
- 自动识别学生信息（学号、姓名、班级）
- 自动识别成绩列（考勤、作业、实验、复习笔记、期末等）
- **支持动态成绩字段识别**（如：点名、电子笔记、作品、报告等）
- 数据预览和验证
- 批量导入成绩数据

**使用场景：**
- 教师上传包含学生成绩的Excel表格
- 系统自动识别表格中的列名，匹配到对应的成绩字段
- 支持自定义字段（如"电子笔记"、"作品"、"报告"等）
- 导入后自动计算最终成绩

**示例：**
```
Excel表格列：学号、姓名、班级、点名、电子笔记、作业成绩、实验、作品、报告
系统会自动识别并导入所有成绩字段
```

### 2. 教学大纲上传与解析 ✅

**功能描述：**
- 支持上传教学大纲文件（PDF、Word格式）
- 自动解析大纲内容，提取课程信息
- 识别成绩评定方式和比例
- 根据大纲内容建议需要的成绩字段

**使用场景：**
- 教师上传课程教学大纲
- 系统解析大纲，提取成绩评定规则
- 自动配置评分政策
- 提示需要录入的成绩类型

### 3. 自定义成绩计算公式 ✅

**功能描述：**
- 支持自定义平时成绩计算公式
- 支持自定义期末成绩计算公式
- 公式支持变量（如：点名、电子笔记、作业成绩、作品、报告等）
- 支持基本数学运算（+、-、*、/、()）
- 公式测试功能

**公式示例：**

1. **平时成绩计算：**
   ```
   平时成绩 = (点名*0.05 + 电子笔记*0.05 + 作业成绩*0.1) / 0.2
   ```
   说明：点名占5%，电子笔记占5%，作业成绩占10%，总权重为20%

2. **期末成绩计算：**
   ```
   期末平均 = (作品 + 报告) / 2
   ```
   说明：作品和报告的平均分作为期末成绩

3. **最终成绩计算：**
   ```
   最终成绩 = 平时成绩 * 平时权重 + 期末成绩 * 期末权重
   ```
   例如：平时30% + 期末70%

**使用场景：**
- 不同课程有不同的成绩计算方式
- 教师可以根据课程特点自定义计算公式
- 系统自动根据公式计算每个学生的成绩

### 4. 成绩管理与编辑 ✅

**功能描述：**
- 成绩列表展示（支持分页、搜索、筛选）
- 支持编辑单个成绩
- 支持动态字段显示和编辑
- 成绩发布/取消发布
- 成绩验证功能

**动态字段支持：**
- 系统自动识别Excel中的额外成绩列
- 在成绩列表中动态显示这些字段
- 支持编辑这些动态字段的值

### 5. 成绩计算引擎 ✅

**功能描述：**
- 自动计算平时成绩（根据公式或权重）
- 自动计算期末成绩（根据公式或权重）
- 自动计算最终成绩
- 自动计算绩点和等级
- 支持批量重新计算

**计算流程：**
1. 读取成绩原始数据（考勤、作业、实验等）
2. 根据配置的公式计算平时成绩
3. 根据配置的公式计算期末成绩
4. 根据权重计算最终成绩
5. 转换为绩点和等级（A、B、C、D、F）

### 6. 成绩统计与分析 ✅

**功能描述：**
- 班级成绩统计（平均分、最高分、最低分、中位数等）
- 及格率、优秀率统计
- 成绩分布分析
- 等级分布统计

### 7. 打印功能 ✅

**功能描述：**
- 生成课程成绩报告（HTML格式）
- 支持打印预览
- 包含完整的成绩表格和统计信息
- 支持动态字段打印
- 打印格式美观，适合打印输出

**打印内容：**
- 课程基本信息
- 统计信息（平均分、及格率等）
- 完整成绩表格（包含所有成绩字段）
- 生成时间

### 8. 用户认证系统 ✅

**功能描述：**
- 教师登录功能
- JWT Token认证
- 权限管理（教师、学生、管理员）
- 用户信息管理

### 9. 课程与班级管理 ✅

**功能描述：**
- 课程创建和管理
- 班级创建和管理
- 学生选课管理
- 评分政策配置

### 10. 学生信息管理 ✅

**功能描述：**
- 学生信息录入
- 学生信息查询和编辑
- 支持批量导入学生信息

## 技术实现

### 后端技术栈
- Django 5.2.5
- Django REST Framework
- MySQL数据库
- JWT认证
- pandas（Excel处理）
- pdfplumber（PDF解析）
- python-docx（Word解析）

### 前端技术栈
- Vue 3
- Element Plus
- Vue Router
- Pinia（状态管理）
- Axios（HTTP请求）

## 数据模型

### 核心模型
1. **User（用户）**：教师、学生、管理员
2. **Course（课程）**：课程基本信息
3. **CourseClass（课程班级）**：课程的具体班级
4. **Score（成绩）**：学生成绩记录
5. **GradingPolicy（评分政策）**：成绩计算规则
6. **ScoreImportLog（导入日志）**：Excel导入记录

### 成绩字段设计
- **标准字段**：考勤、作业、实验、复习笔记、期末
- **动态字段**：存储在`extra_scores` JSON字段中，支持任意字段名
- **计算字段**：平时总分、期末总分、最终成绩、绩点、等级

## 使用流程

### 1. 课程设置
1. 创建课程
2. 上传教学大纲（可选）
3. 创建课程班级
4. 配置评分政策和计算公式

### 2. 成绩录入
1. 准备Excel成绩表
2. 上传Excel文件
3. 预览和确认数据
4. 导入成绩

### 3. 成绩计算
1. 系统自动根据公式计算成绩
2. 可以手动触发重新计算
3. 查看计算结果

### 4. 成绩管理
1. 查看成绩列表
2. 编辑单个成绩（如需要）
3. 发布成绩（学生可见）
4. 导出成绩表

### 5. 打印报告
1. 选择课程班级
2. 生成成绩报告
3. 打印或保存PDF

## 特色功能

### 1. 智能字段识别
系统能够自动识别Excel中的成绩列，即使列名不完全匹配也能识别。

### 2. 动态字段支持
支持任意数量的成绩字段，不需要修改数据库结构。

### 3. 灵活的计算公式
支持复杂的计算公式，满足不同课程的需求。

### 4. 完整的审计功能
记录成绩的创建人、更新人、创建时间、更新时间等。

### 5. 权限控制
不同角色（教师、学生、管理员）有不同的权限。

## 代码统计

- **后端代码**：约6000+行
- **前端代码**：约4000+行
- **总计**：10000+行

## 补充功能建议

除了已实现的功能，系统还可以补充以下功能：

1. **成绩分析图表**
   - 成绩分布直方图
   - 各科目对比图表
   - 趋势分析图表

2. **成绩预警**
   - 低分预警
   - 缺考提醒
   - 成绩异常检测

3. **成绩申诉**
   - 学生成绩申诉功能
   - 申诉审核流程

4. **批量操作**
   - 批量修改成绩
   - 批量发布/取消发布
   - 批量导出

5. **数据备份**
   - 自动备份功能
   - 数据恢复功能

6. **通知功能**
   - 成绩发布通知
   - 成绩更新通知

7. **移动端支持**
   - 响应式设计
   - 移动端适配

8. **数据导入模板**
   - 提供标准Excel模板
   - 模板下载功能

## 部署说明

### 后端部署
1. 安装Python依赖：`pip install -r requirements.txt`
2. 配置数据库连接（settings.py）
3. 运行数据库迁移：`python manage.py migrate`
4. 创建超级用户：`python manage.py createsuperuser`
5. 启动服务：`python manage.py runserver`

### 前端部署
1. 安装依赖：`npm install`
2. 配置API地址（utils/request.js）
3. 构建项目：`npm run build`
4. 部署到Web服务器

## 注意事项

1. Excel文件大小限制：10MB
2. 支持的文件格式：.xlsx, .xls, .pdf, .doc, .docx
3. 成绩范围：0-100分
4. 公式中的变量名必须与Excel列名或系统字段名一致

## 联系方式

如有问题或建议，请联系开发团队。


